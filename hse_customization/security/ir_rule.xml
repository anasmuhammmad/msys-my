<odoo>
  <!-- Deactivate previously restrictive rule so it doesn't affect global Activities -->
  <record id="rule_hse_grant_approval_only" model="ir.rule">
    <field name="active">0</field>
  </record>
</odoo>